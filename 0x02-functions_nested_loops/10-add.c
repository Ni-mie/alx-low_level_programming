#include "main.h"

/**
 * add - Function that adds two integers and returns the result.
 * @i: First Integer
 * @j: Second Integer
 * Return: Always 0 (Success)
 */
int add(int i, int j)
{
	int sum;

	sum = i + j;
	return (sum);
}
